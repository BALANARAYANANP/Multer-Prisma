export  const Types = {
    PrismaClient : Symbol.for("PrismaClient"),
    profileRepository : Symbol.for("profileRepository"),
    profileServices : Symbol.for('profileServices'),
    profileController : Symbol.for('profileController'),
}